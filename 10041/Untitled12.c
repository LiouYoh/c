#include <stdio.h>
int main(){
	int times;
	scanf("%d",&times);
	int number[times][40000];
	
	int loop;
	int tt;

	for(loop =0;loop<times;loop++){
		scanf("%d",&tt);
		int run;
		for(run =0;run< tt;run ++){
		}
	}
}
